#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: pin a-z in reverse
 *
 * Return: Always 0
 */

int main(void)
{
	char ch = 'z';

	while (c >= 'a')
	{
		putchar(ch);
		ch--;
	}
	putchar('\n');

	return (0);
}
